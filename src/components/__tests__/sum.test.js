const { sum } = require("../sum")
// import {sum} from "../sum"

test("Check the sum of two numbers",()=>{
    expect(sum(2,5)).toBe(7)
})


//check for negetive numbers

/* 
    "shift + control + A"
    read about jest
    diffirence between js object and json
    read about coverage report
    remove the warning in shimmer test file 
 */ 

/* 
   install React testing library
   install jest 
   installed jest-environment-jsdom
   create my first test 
   configure babel
   wrote expect sum test
   gitignore coverage report

   wrrote the test cases for Header
*/

